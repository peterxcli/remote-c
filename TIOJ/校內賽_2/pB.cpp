#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

bool flag = false;
int ans = 0;
int n, k;
string ori, target;

int main() {
    printf("%d", ori.size());
    cin >> n >> k;
    for (int i = 0; i < n; i++){
        char temp;
        cin >> temp;
        ori += temp;
    } 
        
    for (int i = 0; i < n; i++){
        char temp;
        cin >> temp;
        target += temp;
    } 
    for (int i = 0; i < n; i++) {
        if(ori[i] > target) {

        }
        else {

        }
    }
    printf("%d\n", ans);
    
}

int search() {
    
}