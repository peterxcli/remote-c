#include <iostream>
#include <math.h>
#include <algorithm>
#include <cstring>
#include <vector>

typedef long long ll;

using namespace std;

int T;

int main() {
    cin >> T;
    ll ans;
    while(T--) {
        int n, m;
        cin >> n >> m;
        if(m < n) {cout << -1 << endl;break;}
        vector<int> fridges[1000];

    }
}